#ifndef USERLANDABLE_UTILS_H
#define USERLANDABLE_UTILS_H

#include <sys/time.h>
#include <string.h>

int pstrcmp(const void *a, const void *b);

long long currentTimestamp() ;

#endif //USERLANDABLE_UTILS_H
